const 
// instead of writing multiple const just call the lines without const
// http is a built in package
  http = require('http'), 
// importing your own packages 
  geometry = require('./geometry.js'), 
  text = require('./text.js')

// console.log(geometry.diameter(3))
console.log(text.reverse("boom"))
